const {
    expect
} = require('chai');
const parser = require('../src/watcher');

describe('Watcher', () => {

    describe('#watch()', () => {
        it('should watch object property and trigger callback when it changes', () => {
            
        });
    });
});